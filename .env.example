# Ottoscaler 환경 설정 템플릿
# 이 파일은 개발자별 환경 설정의 예시입니다
# 
# 실제 사용법:
#   1. make setup-user USER=한진우  # 자동으로 .env.jinwoo.local 생성
#   2. ENV_FILE=".env.jinwoo.local" make run-app  # 환경 파일 지정하여 실행
#
# ⚠️ 이 파일을 직접 수정하지 마세요. setup-user 스크립트가 자동으로 생성합니다.

# Redis 설정 - 개발자별 전용 Redis 인스턴스
REDIS_HOST=localhost
REDIS_PORT=6379                          # 개발자별로 자동 할당 (6379~6384)
REDIS_PASSWORD=
REDIS_DB=0
REDIS_STREAM=otto:scale:events
REDIS_CONSUMER_GROUP=ottoscaler-jinwoo   # 개발자별로 자동 설정
REDIS_CONSUMER=ottoscaler-jinwoo-1       # 개발자별로 자동 설정

# Kubernetes 설정 - 개발자별 네임스페이스
NAMESPACE=jinwoo-dev                     # 개발자별로 자동 설정

# Worker Pod 설정  
OTTO_AGENT_IMAGE=busybox:latest

# 개발자 정보 (setup-user 스크립트에서 자동 설정)
DEVELOPER_NAME=한진우
DEVELOPER_ENG_NAME=jinwoo
DEVELOPER_ID=1

# Kind 클러스터 설정
KIND_CLUSTER_NAME=ottoscaler-jinwoo      # 개발자별로 자동 설정

# 🔧 개발자별 자동 할당 포트:
# - 한진우 (jinwoo): Redis:6379, Cluster:ottoscaler-jinwoo, Namespace:jinwoo-dev
# - 장준영 (junyoung): Redis:6380, Cluster:ottoscaler-junyoung, Namespace:junyoung-dev
# - 고민지 (minji): Redis:6381, Cluster:ottoscaler-minji, Namespace:minji-dev
# - 이지윤 (jiyoon): Redis:6382, Cluster:ottoscaler-jiyoon, Namespace:jiyoon-dev
# - 김보아 (boa): Redis:6383, Cluster:ottoscaler-boa, Namespace:boa-dev
# - 유호준 (hojun): Redis:6384, Cluster:ottoscaler-hojun, Namespace:hojun-dev

# 환경별 설정 예시:
#
# 로컬 개발 (기본값):
# REDIS_HOST=localhost
# REDIS_PORT=6379-6384 (자동 할당)
# 
# Docker Compose:
# REDIS_HOST=redis
#
# Kubernetes (외부 Redis):
# REDIS_HOST=host.docker.internal
#
# 프로덕션 Redis (인증 사용):
# REDIS_HOST=redis.production.com
# REDIS_PASSWORD=your-redis-password
# REDIS_PORT=6380